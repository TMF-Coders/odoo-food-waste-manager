<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.food.waste</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="base.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="inside">
                <app data-string="Food Waste" string="Food Waste" name="food_waste_manager">
                    <block title="Notifications &amp; Alerts" name="food_waste_alerts_setting_container">
                        <setting id="waste_alerts_setting" title="Notify managers on high waste" help="Send automatic emails to managers when waste exceeds a certain threshold.">
                            <field name="module_food_waste_alerts"/>
                            <div class="content-group" invisible="not module_food_waste_alerts">
                                <div class="mt16">
                                    <label for="waste_threshold_cost" class="o_light_label"/>
                                    <field name="waste_threshold_cost" class="oe_inline"/>
                                </div>
                            </div>
                        </setting>
                    </block>
                </app>
            </xpath>
        </field>
    </record>

    <record id="action_food_waste_config_settings" model="ir.actions.act_window">
        <field name="name">Settings</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_mode">form</field>
        <field name="target">inline</field>
        <field name="context">{'module' : 'food_waste_manager'}</field>
    </record>

    <menuitem id="menu_food_waste_settings" 
              name="Settings" 
              parent="food_waste_manager.menu_food_waste_config" 
              action="action_food_waste_config_settings" 
              sequence="0"/>
</odoo>
